specVersion: "v1"
serviceId: "pideas"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pideas-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
